# Release Notes

Each stable version uses a file named `v<MARKETING_VERSION>.md`. The release script writes the same
content into the Sparkle appcast and the GitHub Release.

Never put keys, certificate paths, or notarization credentials in release notes.
